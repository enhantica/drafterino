name: "Drafterino"
description: "Draft GitHub release notes and bump versions based on merged PR labels"

inputs:
  config:
    description: "YAML configuration string defining title, tag, bump rules, and release notes grouping"
    required: true

runs:
  using: "node20"
  main: "dist/index.js"
